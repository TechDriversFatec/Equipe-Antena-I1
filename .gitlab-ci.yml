image: gradle:alpine

services:
    - tutum/mongodb:latest
    - docker:dind

build:
  stage: build
  script: docker run -d -p 27017:27017 --name mongodb tutum/mongodb mongod --smallfiles && ./gradlew --build-cache build
  cache:
    key: "$CI_COMMIT_REF_NAME"
    policy: push
    paths:
      - build
      - .gradle

test:
  stage: test
  script: ./gradlew test
